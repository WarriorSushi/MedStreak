org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# Fix Java version warnings
android.defaults.buildfeatures.buildconfig=true
android.nonTransitiveRClass=false
android.nonFinalResIds=false

# Using Java 8 compatibility for all modules to fix plugin compatibility issues
# org.gradle.java.home=C:\Program Files\Java\jdk-11

# Force Java 8 compatibility for all modules
android.useAndroidX=true
android.enableJetifier=true

# Fix for JVM target compatibility issues
org.gradle.java.installations.auto-detect=false
org.gradle.java.installations.auto-download=false
android.jetifier.ignorelist=kotlin-compiler-embeddable-\d+\.\d+\.\d+\.jar

# Force Java 8 compatibility for all modules (this is what most Flutter plugins expect)
kotlin.jvm.target.validation.mode=ignore
